This folder contains project planning files that are being used to set up and design the CAN software for SC7 on the Arduino Platform. The code is heavily based on the working SC6 CAN system, but needs to work slightly differently because we use an external controller with the Arduino.

- Alexander Martin, Oct. 31, 2014.